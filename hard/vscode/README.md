# VS Code settings

VS Code の設定は Settings Sync を使って管理している。
Settings Sync では "Settings" だけを同期していて、それ以外は同期していない。

`dotfiles/vscode/settings.json` は 2026-04 時点での VS Code の settings.json の内容をバックアップとして保管したもの。
